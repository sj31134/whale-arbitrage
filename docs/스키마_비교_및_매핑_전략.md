# 📊 CSV 파일과 whale_address 테이블 스키마 비교 및 매핑 전략

## 1️⃣ whale_address 테이블 스키마

```sql
CREATE TABLE whale_address (
    id TEXT,                    -- PRIMARY KEY의 일부
    chain_type TEXT,            -- PRIMARY KEY의 일부
    address TEXT,
    name_tag TEXT,
    balance TEXT,
    percentage TEXT,
    txn_count TEXT
);
PRIMARY KEY: (id, chain_type)
```

**샘플 데이터:**
```json
{
  "id": "BSC001",
  "chain_type": "BSC",
  "address": "0xff3f428583c15a5681584e9e5e86e270418ac4d3",
  "name_tag": null,
  "balance": "29,888,000.15537403 BNB",
  "percentage": "19.87%",
  "txn_count": "101"
}
```

---

## 2️⃣ CSV 파일 스키마

### 공통 스키마 (BNB, USDC, XRP)
```csv
rank,address,chain_type,coin_symbol,network
```

**샘플 데이터:**
- **BNB**: `1,0x2804ada1c219e50898e75b2bd052030580f4fbac,BSC,BNB,mainnet`
- **USDC (Ethereum)**: `1,0x5e8af2a74e34fceffafbb2af92c9507b9f81ecb0,ETH,USDC,ethereum`
- **USDC (BSC)**: `1,0x5e8af2a74e34fceffafbb2af92c9507b9f81ecb0,BSC,USDC,bsc`
- **XRP**: `1,r38a3PtqW3M7LRESgaR4dyHjg3AxAmiZCt,XRP,XRP,mainnet`

---

## 3️⃣ 스키마 매핑 전략

### 컬럼 매핑

| whale_address 컬럼 | CSV 컬럼 | 변환 로직 |
|-------------------|---------|----------|
| **id** | `rank` + `chain_type` | `{chain_type}{rank:03d}` (예: BSC001, ETH001, XRP001) |
| **chain_type** | `chain_type` | 그대로 사용 (BSC, ETH, XRP 등) |
| **address** | `address` | 그대로 사용 |
| **name_tag** | `coin_symbol` | 코인 전체 이름으로 변환 (BNB → "Binance Coin", USDC → "USD Coin", XRP → "Ripple") |
| **balance** | 없음 | `NULL` 또는 빈 문자열 |
| **percentage** | 없음 | `NULL` 또는 빈 문자열 |
| **txn_count** | 없음 | `NULL` 또는 빈 문자열 |

---

## 4️⃣ USDC 네트워크별 chain_type 매핑

USDC는 여러 네트워크에 존재하므로 `network` 컬럼을 기반으로 `chain_type`을 결정:

| CSV network | whale_address chain_type | 설명 |
|------------|-------------------------|------|
| `ethereum` | `ETH` | Ethereum 네트워크 |
| `bsc` | `BSC` | Binance Smart Chain |
| `polygon` | `POLYGON` | Polygon 네트워크 |
| `arbitrum` | `ARBITRUM` | Arbitrum 네트워크 |
| `optimism` | `OPTIMISM` | Optimism 네트워크 |
| `avalanche` | `AVALANCHE` | Avalanche 네트워크 |
| `solana` | `SOL` | Solana 네트워크 |
| `base` | `BASE` | Base 네트워크 |

**주의:** USDC의 경우 `coin_symbol`은 모두 `USDC`이지만, `chain_type`은 네트워크에 따라 다릅니다.

---

## 5️⃣ ID 생성 규칙

### BNB
- **chain_type**: `BSC`
- **ID 형식**: `BSC{rank:03d}` (예: BSC001, BSC002, ..., BSC100)
- **주의**: 기존 BSC 데이터와 중복되지 않도록 확인 필요

### USDC
- **chain_type**: 네트워크에 따라 다름 (ETH, BSC, POLYGON 등)
- **ID 형식**: `{chain_type}{rank:03d}` (예: ETH001, BSC001, POLYGON001)
- **주의**: 각 네트워크별로 독립적인 ID 생성

### XRP
- **chain_type**: `XRP`
- **ID 형식**: `XRP{rank:03d}` (예: XRP001, XRP002, ..., XRP100)

---

## 6️⃣ name_tag 생성 규칙

| coin_symbol | name_tag |
|------------|----------|
| `BNB` | `Binance Coin` |
| `USDC` | `USD Coin` |
| `XRP` | `Ripple` |

---

## 7️⃣ 처리해야 할 CSV 파일 목록

### BNB
- `bnb_mainnet_richlist_top100.csv` (100개)

### USDC (8개 네트워크)
- `usdc_ethereum_richlist_top100.csv` (100개)
- `usdc_bsc_richlist_top100.csv` (100개)
- `usdc_polygon_richlist_top100.csv` (100개)
- `usdc_arbitrum_richlist_top100.csv` (100개)
- `usdc_optimism_richlist_top100.csv` (100개)
- `usdc_avalanche_richlist_top100.csv` (100개)
- `usdc_solana_richlist_top100.csv` (100개)
- `usdc_base_richlist_top100.csv` (100개)

### XRP
- `xrp_mainnet_richlist_top100.csv` (100개)

**총 예상 레코드 수:**
- BNB: 100개
- USDC: 800개 (8개 네트워크 × 100개)
- XRP: 100개
- **합계: 1,000개**

---

## 8️⃣ 주의사항

### 중복 ID 방지
- 기존 whale_address 테이블에 이미 존재하는 ID와 중복되지 않도록 확인
- 예: 기존에 `BSC001`이 있으면 새로운 BNB 데이터는 `BSC101`부터 시작하거나 다른 ID 체계 사용

### chain_type 일관성
- USDC의 경우 네트워크별로 다른 `chain_type` 사용
- 기존 테이블의 `chain_type` 값과 일치시켜야 함

### PRIMARY KEY 제약
- `(id, chain_type)` 복합 키이므로, 같은 `id`라도 `chain_type`이 다르면 다른 레코드로 저장 가능
- 예: `BSC001` + `BSC`와 `BSC001` + `ETH`는 다른 레코드

---

## 9️⃣ 구현 계획

1. **CSV 파일 읽기**: 모든 BNB, USDC, XRP CSV 파일 읽기
2. **ID 생성**: `{chain_type}{rank:03d}` 형식으로 ID 생성
3. **chain_type 매핑**: USDC의 경우 `network` → `chain_type` 매핑
4. **name_tag 생성**: `coin_symbol` → 전체 이름 변환
5. **기존 데이터 확인**: Supabase에서 기존 ID 확인하여 중복 방지
6. **데이터 변환**: whale_address 스키마에 맞게 변환
7. **Supabase 업로드**: upsert로 기존 데이터 업데이트 및 새 데이터 추가

